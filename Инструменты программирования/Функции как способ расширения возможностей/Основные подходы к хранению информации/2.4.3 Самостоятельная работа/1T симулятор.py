"""
Реализуйте функцию take_screenshot(), которая сохраняет скриншот
с помощью вызова screenshot.save(title, height, width).
"""


def take_screenshot():
    # Задаем переменные
    title = "screenshot"
    height = 1080
    width = 1920

    # Имитация вызова метода save с указанными параметрами
    screenshot.save(title, height, width)


# Вызываем функцию
take_screenshot()